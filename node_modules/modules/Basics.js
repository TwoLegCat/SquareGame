class Basics {
    constructor(x, y, hp) {
        this.x = x;
        this.y = y;
        this.tile = {
            x: ~~x,
            y: ~~y
        }
        this.hp = hp;
        this.lvl = 0;
        this.atkspd = 1000;
        this.id = (~~(Math.random() * 16777216)).toString(16)
    }
}